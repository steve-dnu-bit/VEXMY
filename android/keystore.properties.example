# Copy to android/keystore.properties (never commit).
# Generate keystore: keytool -genkey -v -keystore android/velbok-release.keystore -alias velbok -keyalg RSA -keysize 2048 -validity 10000

storeFile=velbok-release.keystore
storePassword=CHANGE_ME
keyAlias=velbok
keyPassword=CHANGE_ME
